import {
  fruits,
  users
} from '../variables.js';


console.log(fruits);

("some" + "some")

function getNewArray(initialArray) {
  return initialArray.map(item => ({[item]: item})).filter()
}


const newArary = getNewArray(fruits);
console.log(newArary);
// console.log(!!newArary);
